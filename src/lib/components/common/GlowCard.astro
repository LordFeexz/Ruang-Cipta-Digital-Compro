---
import { cn } from "../../../lib/utils";

interface Props {
  class?: string;
  /**
   * AOS stagger in ms, forwarded as `data-aos-delay`. A Tailwind
   * `delay-[${i * 100}ms]` class built from an interpolated value is never
   * generated by the v4 scanner, so the previous approach silently did nothing.
   */
  delay?: number;
}

const { class: className = "", delay } = Astro.props;
---

<div
  class={cn(
    "group bg-card border-border relative h-full overflow-hidden rounded-2xl border p-6 transition-colors duration-200 sm:p-8",
    "hover:border-brand-ring hover:bg-accent",
    className,
  )}
  data-aos="fade-up"
  data-aos-duration="500"
  data-aos-delay={delay}
>
  <slot />
</div>
